name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  UV_VERSION: "0.12.3"

# One run per ref: a new push supersedes the previous matrix instead of queuing
# behind it. Without this, consecutive pushes each hold a full Windows matrix
# (about an hour per job) and later runs wait for capacity they cannot get.
concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gitleaks:
    name: timing::focused::gitleaks
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
          # The action scans a commit range; a depth-1 clone can leave the
          # earlier commits of a push unscanned. 81 MB of history, 10-minute job.
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_VERSION: 8.30.1

  lint:
    name: timing::focused::lint
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22.23.1"
      - run: uv sync --locked --python 3.14
      - run: uv run --locked --no-sync ruff check scripts/ tests/ benchmark/
      - run: uv run --locked --no-sync python -m compileall -q -x "tests[\\/]fixtures[\\/]code_kernel[\\/]python[\\/]pkg[\\/]broken[.]py" scripts tests benchmark
      - run: uv run --locked --no-sync python scripts/lint_memory.py --scope all --fail-on-findings --allowed-categories orphan_daily_logs missing_sources_section temporal_validity
      - run: node --check scripts/llm-wiki-memory-opencode.js
      - run: bash -n install.sh
      # Nothing parsed PowerShell and nothing ran shellcheck: a syntax error in
      # install.ps1 reached a release, and install.sh was never checked beyond
      # `bash -n`. The owner's complexity gate stays out: it is machine-local
      # tooling under ~/.claude, not part of this repository.
      # See docs/research/2026-09-17-the-remaining-ci-gaps-of-the-third-audit.md.
      - run: shellcheck install.sh
      - name: Every tracked PowerShell file parses
        shell: pwsh
        run: |
          $failed = $false
          foreach ($file in (git ls-files '*.ps1')) {
              $errors = $null
              $tokens = $null
              [System.Management.Automation.Language.Parser]::ParseFile(
                  (Resolve-Path -LiteralPath $file).Path, [ref]$tokens, [ref]$errors) | Out-Null
              if ($errors.Count) { $failed = $true; Write-Host "$file`n$($errors | Out-String)" }
          }
          if ($failed) { exit 1 }

  pytest-full:
    name: timing::${{ matrix.class }}::py${{ matrix.python }}-s${{ matrix.shard }}
    strategy:
      fail-fast: false
      matrix:
        # The suite is split across four shards. A single Windows job took 57
        # minutes, and a run that long starves its own concurrency tests; each
        # shard is a separate runner with its own cores, so the split shortens
        # the wall clock without adding contention on one machine. Setup is
        # about half a minute, so the extra jobs cost almost nothing.
        target:
          - linux-py3.10
          - linux-py3.14
          - windows-py3.10
          - windows-py3.11
          - windows-py3.12
          - windows-py3.13
          - windows-py3.14
          - macos-py3.10
          - macos-py3.14
        shard: [1, 2, 3, 4]
        include:
          - target: linux-py3.10
            os: ubuntu-24.04
            python: "3.10"
            timeout: 20
            class: linux_full
          - target: linux-py3.14
            os: ubuntu-24.04
            python: "3.14"
            timeout: 20
            class: linux_full
          - target: windows-py3.10
            os: windows-2025
            python: "3.10"
            timeout: 60
            class: windows_full
          - target: windows-py3.11
            os: windows-2025
            python: "3.11"
            timeout: 60
            class: windows_full
          - target: windows-py3.12
            os: windows-2025
            python: "3.12"
            timeout: 60
            class: windows_full
          - target: windows-py3.13
            os: windows-2025
            python: "3.13"
            timeout: 60
            class: windows_full
          - target: windows-py3.14
            os: windows-2025
            python: "3.14"
            timeout: 60
            class: windows_full
          - target: macos-py3.10
            os: macos-15
            python: "3.10"
            timeout: 20
            class: macos_full
          - target: macos-py3.14
            os: macos-15
            python: "3.14"
            timeout: 20
            class: macos_full
    runs-on: ${{ matrix.os }}
    timeout-minutes: ${{ matrix.timeout }}
    env:
      UV_PROJECT_ENVIRONMENT: ${{ github.workspace }}/../llm-wiki-full-${{ matrix.class }}-py${{ matrix.python }}
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-state-${{ matrix.class }}-py${{ matrix.python }}-s${{ matrix.shard }}
      LLM_WIKI_TEST_USE_EXTERNAL_STATE: "1"
      MEMORY_LLM_PROVIDER: fake
      # A native crash then prints the Python stack it died on. On 2026-09-13
      # windows_full py3.11-s2 died after 2 058 of 2 080 tests with no summary,
      # no junit.xml and no traceback (run 34727708815), which named nothing.
      PYTHONFAULTHANDLER: "1"
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - run: uv python install ${{ matrix.python }}
      - run: uv python pin ${{ matrix.python }}
      - run: uv sync --locked
      # `${{ github.workspace }}/../…` keeps runtime state out of the checkout but
      # leaves a `..` component. The Windows handle-relative walker refuses to
      # traverse one by design, so normalize the path once for the whole job.
      - name: Normalize the external state root
        shell: bash
        run: >
          uv run --locked --no-sync python -c
          "import os, pathlib;
          print('LLM_WIKI_STATE_ROOT=' + str(pathlib.Path(os.environ['LLM_WIKI_STATE_ROOT']).resolve()))"
          >> "$GITHUB_ENV"
      # The name of the test a killed process was running comes from
      # LLM_WIKI_TEST_PROGRESS_FILE, one line per test, uploaded below. `-v` was
      # tried first and cost two runs: a 40-minute and then a 60-minute cap on
      # Windows shards that normally take 22-26 minutes.
      - run: >
          uv run --locked --no-sync python -m tests.shard_plan
          --shard ${{ matrix.shard }} --of 4 --
          -q --durations=0 --junitxml="${{ runner.temp }}/pytest-timings/junit.xml"
        env:
          LLM_WIKI_TEST_PROGRESS_FILE: ${{ runner.temp }}/pytest-timings/progress.txt
      - if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pytest-timings-${{ matrix.class }}-py${{ matrix.python }}-s${{ matrix.shard }}-attempt-${{ github.run_attempt }}
          path: ${{ runner.temp }}/pytest-timings/
          if-no-files-found: error

  clean-production:
    name: timing::clean::production-${{ matrix.label }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            python: "3.10"
            label: py3.10
          - os: ubuntu-24.04
            python: "3.14"
            label: py3.14
          # A clean production install on the two other platforms the
          # contract names had no CI evidence (audit OPS-14, 2026-09-11).
          - os: windows-2025
            python: "3.13"
            label: windows-py3.13
          - os: macos-15
            python: "3.13"
            label: macos-py3.13
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    env:
      UV_PROJECT_ENVIRONMENT: ${{ github.workspace }}/../llm-wiki-production-${{ matrix.label }}
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-production-state-${{ matrix.label }}
      MEMORY_LLM_PROVIDER: fake
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - run: uv python install ${{ matrix.python }}
      - run: uv python pin ${{ matrix.python }}
      - run: uv sync --locked --no-default-groups
      - run: uv run --locked --no-sync python -c "import importlib.util; assert importlib.util.find_spec('pytest') is None"
      - run: uv run --locked --no-sync python -c "import os; from pathlib import Path; Path(os.environ['LLM_WIKI_STATE_ROOT']).mkdir(parents=True, exist_ok=True)"
      - run: uv run --locked --no-sync python scripts/install_smoke.py --deadline-seconds 120

  clean-hybrid:
    name: timing::clean::hybrid-py${{ matrix.python }}
    strategy:
      fail-fast: false
      matrix:
        python: ["3.10", "3.14"]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      UV_PROJECT_ENVIRONMENT: ${{ github.workspace }}/../llm-wiki-hybrid-${{ matrix.python }}
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-hybrid-state-${{ matrix.python }}
      MEMORY_LLM_PROVIDER: fake
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - run: uv python install ${{ matrix.python }}
      - run: uv python pin ${{ matrix.python }}
      - run: uv sync --locked --no-default-groups --extra hybrid
      - run: uv run --locked --no-sync python -c "import importlib.util; assert importlib.util.find_spec('pytest') is None"
      - run: uv run --locked --no-sync python -c "import os; from pathlib import Path; import numpy as np; path=Path(os.environ['LLM_WIKI_STATE_ROOT'])/'vector.npy'; path.parent.mkdir(parents=True, exist_ok=True); np.save(path, np.array([1,2,3])); assert np.load(path).tolist()==[1,2,3]"
      # The extra exists for this package and for search; the job used to prove
      # only that NumPy round-trips an array. Model weights are not fetched here:
      # the read path is local-only and says so when they are absent.
      - run: uv run --locked --no-sync python -c "import sentence_transformers"
      - run: uv run --locked --no-sync python scripts/search_memory.py "installation"

  clean-code-graph:
    name: timing::clean::code-graph-py${{ matrix.python }}
    strategy:
      fail-fast: false
      matrix:
        python: ["3.10", "3.14"]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      UV_PROJECT_ENVIRONMENT: ${{ github.workspace }}/../llm-wiki-code-graph-${{ matrix.python }}
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-code-graph-state-${{ matrix.python }}
      MEMORY_LLM_PROVIDER: fake
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - run: uv python install ${{ matrix.python }}
      - run: uv python pin ${{ matrix.python }}
      - run: uv sync --locked --no-default-groups --extra code-graph
      - run: uv run --locked --no-sync python -c "import importlib, importlib.util; modules=('jedi','tree_sitter','tree_sitter_python','tree_sitter_javascript','tree_sitter_typescript','tree_sitter_go','tree_sitter_rust','tree_sitter_java','tree_sitter_c','tree_sitter_cpp','tree_sitter_ruby','tree_sitter_php','tree_sitter_c_sharp','tree_sitter_bash'); assert importlib.util.find_spec('pytest') is None; [importlib.import_module(name) for name in modules]"
      - run: uv run --locked --no-sync python scripts/code_graph.py tests/fixtures/code_kernel/python

  installer:
    name: timing::installer::${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            platform: linux
          - os: windows-2025
            platform: windows
          # The LaunchAgent path (install_control.py) had no CI evidence
          # (audit OPS-14, 2026-09-11); a regression there merged green.
          - os: macos-15
            platform: macos
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    env:
      UV_PROJECT_ENVIRONMENT: ${{ github.workspace }}/../llm-wiki-installer-${{ matrix.platform }}
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-installer-state-${{ matrix.platform }}
      LLM_WIKI_TEST_USE_EXTERNAL_STATE: "1"
      MEMORY_LLM_PROVIDER: fake
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - run: uv python install 3.13
      - run: uv python pin 3.13
      - run: uv sync --locked
      - run: uv run --locked --no-sync pytest tests/test_installer_bootstrap.py tests/test_installer_config.py tests/test_install_smoke.py -q
      - run: uv run --locked --no-sync pytest tests/test_integration_injection.py -q -k installer
      - run: uv run --locked --no-sync pytest tests/test_dependency_environments.py -q -k installer

  # The jobs above drive pieces of the installers with fake runners; none of them
  # ever ran an installer. Three defects lived behind that gap at once: a fresh
  # vault never adopted the queue (issue #17 twice), the Windows installer had no
  # adoption step, and the POSIX one used bash 4 syntax that stock macOS lacks. This
  # job runs the real thing, under a HOME and a state root of its own, and then asks
  # the vault whether session capture is actually enabled.
  #
  # `--scheduler cron` is the explicit degraded fallback and the only backend a
  # hosted runner can register: ubuntu-24.04 has no systemd user manager, and the
  # macOS LaunchAgent domain needs an Aqua session. The native backends stay covered
  # by the `installer` job above. macOS runs the installer with `/bin/bash`, which is
  # bash 3.2 — the shell the contract names.
  # See docs/research/2026-09-17-a-real-install-is-run-in-ci.md.
  install-end-to-end:
    name: timing::installer::end-to-end-${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            platform: linux
          - os: macos-15
            platform: macos
          - os: windows-2025
            platform: windows
    runs-on: ${{ matrix.os }}
    # The ceiling `scripts/ci_timing_report.py` records for the `installer` class.
    timeout-minutes: 20
    env:
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-e2e-state-${{ matrix.platform }}
      UV_PROJECT_ENVIRONMENT: ${{ github.workspace }}/../llm-wiki-e2e-env-${{ matrix.platform }}
      MEMORY_LLM_PROVIDER: fake
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      # Created before HOME moves: the steps above install into the runner's own home.
      - name: A home of its own, so nothing of the runner's is configured
        shell: bash
        run: mkdir -p "${{ runner.temp }}/installer-home"
      - name: Run install.sh end to end
        if: runner.os != 'Windows'
        env:
          HOME: ${{ runner.temp }}/installer-home
        run: /bin/bash ./install.sh --scheduler cron
      - name: Run install.ps1 end to end
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          USERPROFILE: ${{ runner.temp }}\installer-home
        run: pwsh -NoProfile -ExecutionPolicy Bypass -File ./install.ps1
      - name: The installed vault reports session capture enabled
        shell: bash
        env:
          HOME: ${{ runner.temp }}/installer-home
          USERPROFILE: ${{ runner.temp }}\installer-home
        run: uv run --locked --no-sync python scripts/repair_installed_memory.py --check --json

  # Tests whose dependency no job installed, so they skipped everywhere and a skip
  # is not a pass. `torch` stays out on purpose: the locked wheel pulls the CUDA
  # stack (73 nvidia packages in uv.lock), gigabytes on every pull request for two
  # test files; a CPU-only pin is a lock change, not a CI change.
  # See docs/research/2026-09-17-the-remaining-ci-gaps-of-the-third-audit.md.
  lexical-and-typescript:
    name: timing::focused::lexical-and-typescript
    runs-on: ubuntu-24.04
    # The ceiling `scripts/ci_timing_report.py` records for the `focused` class.
    timeout-minutes: 15
    env:
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-lexical-state
      LLM_WIKI_TEST_USE_EXTERNAL_STATE: "1"
      LLM_WIKI_LSP_TEST_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-typescript-state
      MEMORY_LLM_PROVIDER: fake
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "22.23.1"
      - run: uv python install 3.13
      - run: uv python pin 3.13
      - run: uv sync --locked --extra lexical-benchmark
      - run: uv run --locked --no-sync pytest tests/test_retrieval_v2_benchmark.py -q -k jieba
      # The explicit operator action, with its pinned tarballs and SRI hashes.
      - run: uv run --locked --no-sync python scripts/install_language_server.py --state-root "${{ env.LLM_WIKI_LSP_TEST_STATE_ROOT }}" --profile typescript
      - run: uv run --locked --no-sync pytest tests/test_typescript_navigation.py -q

  pyright-navigation:
    name: timing::focused::pyright-${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            platform: linux
            python: "3.10"
            node: "22.23.1"
            timeout: 15
          # The hosted Windows image runs this suite roughly three times slower
          # than Linux, and the correctness gate follows it inside the same job.
          - os: windows-2025
            platform: windows
            python: "3.10"
            node: "22.23.1"
            timeout: 40
          - os: macos-15
            platform: macos
            python: "3.10"
            node: "22.23.1"
            timeout: 25
    runs-on: ${{ matrix.os }}
    timeout-minutes: ${{ matrix.timeout }}
    env:
      UV_PROJECT_ENVIRONMENT: ${{ github.workspace }}/../llm-wiki-pyright-${{ matrix.platform }}
      LLM_WIKI_ROOT: ${{ github.workspace }}
      LLM_WIKI_STATE_ROOT: ${{ github.workspace }}/../llm-wiki-state
      LLM_WIKI_TEST_USE_EXTERNAL_STATE: "1"
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: "0.12.3"
          enable-cache: true
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: ${{ matrix.node }}
      - run: uv python install ${{ matrix.python }}
      - run: uv python pin ${{ matrix.python }}
      - run: uv sync --locked
      # `${{ github.workspace }}/../llm-wiki-state` keeps runtime state out of the
      # checkout, but leaves a `..` component. `install_pyright.py` rejects that by
      # design, so normalize the path once and reuse it for the whole job.
      - name: Normalize the external state root
        shell: bash
        run: >
          uv run --locked --no-sync python -c
          "import os, pathlib;
          print('LLM_WIKI_STATE_ROOT=' + str(pathlib.Path(os.environ['LLM_WIKI_STATE_ROOT']).resolve()))"
          >> "$GITHUB_ENV"
      - name: Explicit Pyright install
        run: uv run --locked --no-sync python scripts/install_pyright.py --state-root "${{ env.LLM_WIKI_STATE_ROOT }}"
      - name: Protocol, process-tree, and security tests
        run: uv run --locked --no-sync pytest tests/test_lsp_protocol.py tests/test_lsp_process.py tests/test_lsp_process_tree.py tests/test_lsp_security.py tests/test_pyright_session.py tests/test_code_navigation.py tests/test_code_navigation_renderer.py -q
      - name: Correctness benchmark gate
        run: uv run --locked --no-sync python benchmark/run_code_navigation.py --fixture --correctness-only --require-gates
      - name: Fixed 100 KLOC Python qualification gate
        if: matrix.os == 'ubuntu-24.04'
        run: uv run --locked --no-sync python benchmark/run_code_navigation.py --fixture --qualification --require-gates

  # Branch protection requires status checks by name. Naming all forty-nine
  # matrix jobs by hand is both tedious and silently wrong the moment the
  # matrix changes: a job added later is simply not required, and a red one
  # merges. This job depends on every other job, fails on any result that is
  # not `success`, and is the only name the protection rule has to know.
  # `if: always()` is what makes a skipped or cancelled dependency reach this
  # check instead of skipping it too — a skipped required check never turns
  # the pull request green, but a silently skipped gate would.
  all-green:
    name: all-green
    if: always()
    needs:
      - gitleaks
      - lint
      - pytest-full
      - clean-production
      - clean-hybrid
      - clean-code-graph
      - installer
      - install-end-to-end
      - lexical-and-typescript
      - pyright-navigation
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Refuse any result that is not a clean pass
        run: |
          # GitHub expressions accept single-quoted strings only: `" "` here is
          # what made the whole workflow file unparsable, so no job ran at all.
          results="${{ join(needs.*.result, ' ') }}"
          echo "job results: ${results}"
          for result in ${results}; do
            [ "${result}" = "success" ] && continue
            echo "::error::a required job reported ${result}"
            exit 1
          done
